// src/pages/ReportForm.jsx
// TODO (FE Dev): implement citizen waste report form
// - Issue type selector (icon grid)
// - Severity selector (3-level)
// - GPS location (auto-detect + manual pin fallback)
// - Optional photo upload
// - Submit → POST /api/v1/reports → redirect to /report/done

export default function ReportForm() {
  return <div>TODO: Report Form</div>;
}
